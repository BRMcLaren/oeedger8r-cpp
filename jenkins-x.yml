buildPack: C++
pipelineConfig:
  pipelines:
    pullRequest:
      pipeline:
        agent:
          image: gcr.io/jenkinsxio/builder-go
        stages:
          - name: release
            steps:
              - name: helm-build
                command: echo "hello"
          - name: release2
            steps:
              - name: helm-build
                command: make